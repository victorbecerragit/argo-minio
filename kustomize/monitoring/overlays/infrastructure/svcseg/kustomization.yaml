apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base

patches:
  - path: tenant-svcseg-patch.yaml
    target:
      kind: ServiceMonitor
      name: minio-tenant-svcseg-sm
  # Or another patch for a different namespace

