apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base

patches:
  - path: tenant-mkpl-patch.yaml
    target:
      kind: ServiceMonitor
      name: minio-tenant-mkpl-sm
  # Or another patch for a different namespace

