apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base

patches:
  - path: tenant-acme-patch.yaml
    target:
      kind: ServiceMonitor
      name: minio-tenant-acme-sm
  # Or another patch for a different namespace  