apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minio-tenant-acme-sm
spec:
  selector:
    matchLabels:
      v1.min.io/tenant: tenant-acme
  namespaceSelector:
    matchNames:
      - minio-tenant-acme
